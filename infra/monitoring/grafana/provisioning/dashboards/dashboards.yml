apiVersion: 1

providers:
  - name: "MwalimuKit dashboards"
    orgId: 1
    folder: "MwalimuKit"
    folderUid: "mwalimukit"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards/definitions
